{
  "name": "shellsense",
  "version": "0.1.0",
  "description": "Claude Code PreToolUse hook that explains commands before execution",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
